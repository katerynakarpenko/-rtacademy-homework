# -rtacademy-homework 

#  Tasks 

#   CSS 

# 3.1
# 1. Створіть файл з таблицею стилів (наприклад styles.css) і підключіть як зовнішню таблицю  стилів на кожну html-сторінку
# 2. Додайте правила в вашу таблицю стилів, що праворуч. Якщо все виконали коректно - всі елементи <h1> стануть синього кольору та 25px у висоту.
# 3. Перевірте html-сторінкі на помилки за допомогою HTML-валідатора validator.w3.org
# 4. Перевірте файл стилів на помилки за допомогою CSS-валідатора jigsaw.w3.org/css-validator



# 3.2
# 1. Додайте правила в вашу таблицю стилів: 
#    a. Всі елементи <h1> повинні бути сірого кольору і іншим шрифтом та стилем.
#    b. Тільки в блоці <nav> всі посилання не повинні мати підкреслення.
#    c. У блоці <nav> повинне бути посилання на поточну сторінку, але іншим стилем (будь-яким на вибір).
#    d. У <footer> будь-який текст повинен бути розміром 10px (вказати в одиницях "em").
# 2. Перевірте сторінки на помилки за допомогою HTML-валідатора validator.w3.org
# 3. Перевірте файл стилів на помилки за допомогою CSS-валідатора jigsaw.w3.org/css-validator



# 3.3
# 1. На окремій сторінці створіть блок шириною 400px, автоматичною висотою, кольором фону #f1faee, кольором границі #aad9dc, кольором шрифта #457b9d та шрифтом Arial. Також з відступами та границями як зображено на схематичному малюнку праворуч



# 3.4
# 1. Для блока з завдання #3.3 встановіть наступні стилі:
#            .box {
#                width: 640px;
#                margin: 0;
#                padding: 40px;
#                border: none;
#                background-color: #457b9d;
#                color: #ffffff;
#                font-family: Arial, sans-serif;
#             }
# 2. Додайте мета-тег viewport
# 3. Використовуючи media-запити необхідно змінювати фон цього блока в залежності від ширини екрана (перевіряти краще за допомогою емуляції пристроїв у Chrome DevTools: F12 -> Ctrl+Shift+M): a. Починаючи з 768px колір фона має бути #e9c46a (Orange Yellow Crayola) b. З 992px  колір фона має бути #f4a261 (Sandy Brown) c. З 1200px і більше - #e76f51 (Burnt Sienna)



# 3.5
# 1. Перетворіть значення у px в одиниці відносної довжини
# 2. Додайте підтримку media-запитів для чутливої (responsive) верстки (у відносних одиницях).
# 3. Перевірити за допомогою інструменту від Гугл search.google.com/test/mobile-friendly (у цьому випадку - вставляючи код, а не адресу посилання на файл)



# 3.6
# Спробуйте всі види компонування сторінок за допомогою CSS



# 3.7
# Зверстайте першу сторінку вашого блогу зі списку дизайнів справа (або оберіть бажаний дизайн вашого блогу самі).
#    Вимоги:
#    ● без використання фреймворку
#    ● чутлива (responsive) верстка
#    ● підтримка версії для друку
#    ● всі значення в одиницях відносної довжини (%, em, vw, vh і т.п.)
#    ● кольори у hex
#    ● проходить перевірку без помилок HTML та CSS-валідаторами
#    ● підтримка різних пристроїв
#    ● mobile first
#    ● мікроформати


# 3.8
# Зверстайте сторінку перегляду одного запису/поста зі списку дизайнів справа (або оберіть бажаний дизайн вашого блогу самі).
#    Вимоги:
#    ● без використання фреймворку
#    ● чутлива (responsive) верстка
#    ● підтримка версії для друку
#    ● всі значення в одиницях відносної довжини (%, em, vw, vh і т.п.)
#    ● кольори у hex
#    ● проходить перевірку без помилок HTML та CSS-валідаторами
#    ● підтримка різних пристроїв
#    ● mobile first
#    ● мікроформати


# 4.1
# 1.Створіть сторінку 4.1.1.html, вставте на сторінку в елемент <body> скрипт, наведений праворуч
# 2. Створіть сторінку 4.1.2.html, підключіть зовнішній скрипт (з будь-яким імʼям, наприклад 4.1.2.js) до цієї сторінки з вмістом прикладу праворуч, але без тега <script> 
# 3. Змініть текст повідомлення в прикладі на будь-яке інше

# 4.2
# Напишіть код, що буде виправляти регістр для вказаного значення змінної cityName. Перший символ повинен бути з великої літери, інші - маленькими. Результат повинен бути в змінної cityNameCapitalized і виведений в консолі браузера.

# 4.3
# Поміняйте значення двох змінних з типом Number місцями без використання третьої змінної. Примітка. Спробуйте не гуглити рішення. let a = 10,  b = 20; // ваш код пишіть тут console.log( a, b ); // => 20, 10

# 4.4
# Використовуючи оператор if, напишіть код, що буде виводити в консолі браузера знак числа змінної num:
# ● '+' - якщо значення більше нуля,
# ● '-' - якщо значення менше нуля,
# ● '0' - якщо значення дорівнює нулю.

# 4.5
# Перепишіть завдання #4.4 з використанням тернарного оператора.

# 4.6
# Перепишіть завдання #4.4 з використанням конструкції switch

# 4.7
# 1. Використовуючи цикл for виведіть тільки парні числа з діапазону від 1 до 32 в консоль браузера з наступними умовами:
# a. Всі кратні “4” повинні мати префікс "*".
# b. Всі кратні “10” повинні мати префікс "^"
# 2. Зробіть альтернативний варіант цього завдання використовуючи цикл while

# 4.8
# Напишіть функцію capitalize(), що буде змінювати регістр текста для переданого значення назви міста в функцію на тип "Capitalize" (як в завданні #4.2) і повертати вже правильний результат.

# 4.9
# Змініть вашу функцію capitalize() із завдання #4.8 так, щоб вона також успішно працювала з такими містами як:
# ● Київ
# ● Нью-Йорк
# ● Лос-Анджелес
# ● Ріо-де-Жанейро
# ● Франкфурт-на-Майні
# ● Сен-Сатюрнен-лез-Апт

# 4.10
# Напишіть функцію getSign(), що буде повертати знак числа, переданого першим параметром (завдання #4.4), відносно умови:
# ● "+" - для додатніх чисел;
# ● "-" - для відʼємних чисел;
# ● порожній рядок ("") - для нуля

# 4.11
# Напишіть функцію getRandomInt(), що буде повертати випадкове ціле число в діапазоні від min до max, що будуть її аргументами/параметрами.

# 4.12
# Написати функцію parseCSV(), що розбере (розпарсить) текст у вигляді CSV та поверне результат у вигляді масиву об'єктів. CSV-текст записано через кому та передається в якості аргумента функції. Складові кожного рядка тексту мають бути збережено за наступними ключами "city", "latitude", "longitude", "country", "population". Зверніть увагу на типи даних у значеннях.
# Приклад відповіді:
# [
#  {
#  "city" : "Kyiv",
#  "latitude" : 50.4334,
#  "longitude" : 30.5166,
#  "country" : "Ukraine",
#  "population" : 2709000
#  },
#  // …
# ]

# 4.13
# Створіть стрілкову рекурсивну функцію sum(), що буде підраховувати суму всіх чисел в заданому діапазоні.
# console.log( sum( 1, 3 ) ); // 6
# console.log( sum( 1, 10 ) ); // 55
# console.log( sum( 100, 1000 ) ); // 495550

# 4.14
# 1. Створіть форму калькулятора на HTML+CSS, що зображено нижче.
# 2. Користувач буде вводити символи в текстові поля "Операнд1" і "Операнд2" та обирати необхідну дію з доступних в списку "Дія". Калькулятор має виводити результат обрахунку в текстове поле "Результат" при кліку по кнопці "=". 
# 3. Необхідно відображати червону рамку для полей з введеними помилковими значеннями (наприкад при незаповнені поля або введені некоректних символів).

# 4.15
# Додайте до перевірки введених символів у завданні #4.14 регулярні вирази, для коректного виявлення і відкидання недозволених символів.

# 4.16
# 1. Створіть форму для обрахунку різниці між двома датами та часом на HTML+CSS, що зображено нижче.
# 2. Користувач буде обирати початкові та кінцеві дати і час в полях типу datetime-local та натискати кнопку “Розрахувати різницю”. Результат має виводитись у блок “Різниця між датами”, динамічно його створюючи (видаляти та створювати знову у випадку існування).
# 3. Необхідно відображати червону рамку для полей з введеними помилковими значеннями (наприкад при незаповнені поля або введені початкової дати більшою за кінцеву, введено 31 лютого та ін.)

# 4.17
# За допомогою AJAX завантажте файл https://rtacademy.net/countries.json трьома способами (XMLHttpRequest, jQuery, fetch) 
# 2. Динамічно створіть на сторінці елемент <input> та повʼязаний з ним елемент <datalist> зі списком країн з завантаженого файла.

# 4.18
# 1. Створіть JSON-файл posts.json зі списком постів/записів для першої сторінки вашого блогу (використовуючи за основу наведений приклад) та додайте його директорію проекта.
# 2. Отримайте за допомогою AJAX цей файл на першій сторінці вашого блогу при натисканні на кнопку/посилання “Load More” (або аналогічну).
# 3. Динамічно додайте у необхідне місце на сторінку ці записи (використовуючи функції роботи з DOM createElement/createTextNode)

# 4.19
# Створити нову сторінку, що стане базовою для додавання (а згодом і для редагування) запису блога post_add.html
# 2. Додати на сторінку форму (поки що з методом GET) з наступними полями та правилами до них:
# ● Назва (текстове поле; будь-які букви, цифри, спецсимволи; довжина від 2 до 128 символів; обовʼязкове)
# ● Вміст (текстове багаторядкове поле; будь-які букви, цифри, спецсимволи; довжина від 2 до 65536 символів; обовʼязкове)
# ● Дата публікації (поле вибору дати та часу; може бути дата у майбутньому; обовʼязкове)
# ● Категорія (поле вибору одної категорії зі списку існуючих категорій; обовʼязкове)
# ● Фотографія/cover (поле вибору одного файла; тільки JPEG; розмір до 10МБ; обовʼязкове)
# ● Кнопка “Надіслати” (кнопка Submit; на onSubmit виклик функції валідації п.3)
# 3. Функція валідації (перевірки) має повертати true у випадку успішного проходження і false в інших випадках. При виникненні помилки над/під полем форми має відображатися блок з текстом помилки, а поле мати червону границю. При коректному значенні - поле повинно мати зелену границю, а блок з текстом помилки автоматично видалятися. 

# 4.20
# 1. Створити нову сторінку, що стане базовою для додавання (а згодом і для редагування) категорії для записів блога category_add.html
# 2. Додати на сторінку форму (поки що з методом GET) з наступними полями та правилами до них:
#    ● Назва (текстове поле; будь-які букви, цифри, спецсимволи; довжина від 2 до 32 символів; обовʼязкове)
#    ● Аліас (текстове поле; тільки латинські букви в нижньому регістрі, цифри, “-”; довжина від 2 до 32 символів; обовʼязкове; заповнюється автоматично, транслітеруючи поле “Назва” тільки при її зміні; використовується для адресного рядка)
#    ● Кнопка “Надіслати” (кнопка Submit; на onSubmit виклик функції валідації п.3)
# 3. Функція валідації (перевірки) має повертати true у випадку успішного проходження і false в інших випадках. При виникненні помилки над/під полем форми має відображатися блок з текстом помилки, а поле мати червону границю. При коректному значенні - поле повинно мати зелену границю, а блок з текстом помилки автоматично видалятися. 

# 4.21
# 1. Створити нову сторінку, що буде використовуватись для авторизації автора у блог login.html
# 2. Додати на сторінку форму (поки що з методом GET) з наступними полями та правилами до них:
#    ● Логін (текстове поле; тільки латинські букви, цифри, “-” та “_”; довжина від 2 до 32 символів; обовʼязкове)
#    ● Пароль (поле введення пароля; будь-які букви, цифри, спецсимволи; довжина від 8 до 32 символів; обовʼязкове)
#    ● Кнопка “Надіслати” (кнопка Submit; на onSubmit виклик функції валідації п.3)
# 3. Функція валідації (перевірки) має повертати true у випадку успішного проходження і false в інших випадках. При виникненні помилки над/під полем форми має відображатися блок з текстом помилки, а поле мати червону границю. При коректному значенні - поле повинно мати зелену границю, а блок з текстом помилки автоматично видалятися. 

# 4.22
# 1. Створіть структуру класів за наступною діаграмою (UML-діаграмою): https://rtacademy.net/04.22.png
# 2. Модифікуйте завдання #4.18 -- тепер після отримання JSON з постами/записами створюйте нові екземпляри класу Post (по окремому для кожного поста) та використовуйте їх методи (наприклад getTitle(), getPublishDate(), getCover()) при динамічному додаванні цих елементів в DOM
##### tips
# 1. Створюйте спочатку екземпляри Author, Category, Cover, а тільки після них - Post. Оскільки для методів setAuthor(), setCategory(), setCover() вже необхідно існуючі екземпляри цих класів.
# 2. Для отримання, наприклад, URL для категорії, використовуйте ланцюжок (chain) методів.